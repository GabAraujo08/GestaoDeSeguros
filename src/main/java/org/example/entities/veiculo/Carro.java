package org.example.entities.veiculo;

import org.example.entities.enums.Marca;

public interface Carro extends Veiculo {
    //Atualmente não existe nenhuma especificidade para a classe Carro, porém, futuramente, pode ser que exista essa necessidade.
}
